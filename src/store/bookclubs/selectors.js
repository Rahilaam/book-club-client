export const getBookClubs = (reduxState) => reduxState.bookClubs.allBookClubs;

export const getBookClubDetails = (reduxState) =>
  reduxState.bookClubs.bookClubDetails;


  export const filterOnGenre=(value)=>(reduxState)=>{
    
  }